{
  "name": "phaser-aquarium",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "npm run validate:content && npm run build && npm run test:regression",
    "validate:content": "node scripts/validate-content.mjs",
    "test:regression": "node scripts/regression-test.mjs"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.4",
    "phaser": "^4.1.0",
    "tailwindcss": "^4.2.4"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.0.10"
  }
}
